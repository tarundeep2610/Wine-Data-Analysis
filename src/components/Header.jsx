import React from "react";

const Header = () => {
  return (
    // Render a header component with a title
    <div className="header">
      <div className="title">Wine Data Analytics</div>
    </div>
  );
};

export default Header;
